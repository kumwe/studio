{
  "contractVersion": "0.1-draft",
  "kind": "schema-profile-vector",
  "id": "schema-profile/empty-combinator",
  "description": "Composition arrays are non-empty so their semantics do not drift across validators.",
  "profile": "studio.profile/schema-property",
  "schema": {
    "type": "object",
    "additionalProperties": false,
    "properties": { "value": { "oneOf": [] } }
  },
  "expect": {
    "outcome": "rejected",
    "code": "invalid-keyword-value",
    "schemaPath": "/properties/value/oneOf"
  }
}
