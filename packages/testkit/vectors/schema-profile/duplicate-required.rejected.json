{
  "contractVersion": "0.1-draft",
  "kind": "schema-profile-vector",
  "id": "schema-profile/duplicate-required",
  "description": "Required property names are unique.",
  "profile": "studio.profile/schema-property",
  "schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["title", "title"],
    "properties": { "title": { "type": "string" } }
  },
  "expect": {
    "outcome": "rejected",
    "code": "invalid-keyword-value",
    "schemaPath": "/required/1"
  }
}
