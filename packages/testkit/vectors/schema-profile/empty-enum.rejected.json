{
  "contractVersion": "0.1-draft",
  "kind": "schema-profile-vector",
  "id": "schema-profile/empty-enum",
  "description": "An enum has at least one unique JSON member as required by Draft 2020-12.",
  "profile": "studio.profile/schema-property",
  "schema": {
    "type": "object",
    "additionalProperties": false,
    "properties": { "tone": { "enum": [] } }
  },
  "expect": {
    "outcome": "rejected",
    "code": "invalid-keyword-value",
    "schemaPath": "/properties/tone/enum"
  }
}
