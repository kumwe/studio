{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.kumwe.org/studio/evidence/review-attestation.schema.json",
  "title": "Studio detached signed review attestation",
  "type": "object",
  "additionalProperties": false,
  "required": ["contractVersion", "issuedAt", "kind", "reviewer", "subject"],
  "properties": {
    "contractVersion": { "const": "0.1-draft" },
    "issuedAt": { "$ref": "#/$defs/rfc3339Timestamp" },
    "kind": { "const": "signed-review-attestation" },
    "reviewer": { "$ref": "#/$defs/reviewer" },
    "subject": {
      "oneOf": [
        { "$ref": "#/$defs/manualSubject" },
        { "$ref": "#/$defs/externalSubject" },
        { "$ref": "#/$defs/bundleSubject" },
        { "$ref": "#/$defs/gateSubject" }
      ]
    }
  },
  "$defs": {
    "bundleId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*(?![\\s\\S])",
      "maxLength": 120
    },
    "bundleSubject": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "bundleId",
        "candidateCommit",
        "candidateTree",
        "decision",
        "execution",
        "freshnessExpiresAt",
        "intakeExecutions",
        "kind",
        "reviewedAt",
        "subjectChecksum",
        "workPackage"
      ],
      "properties": {
        "bundleId": { "$ref": "#/$defs/bundleId" },
        "candidateCommit": { "$ref": "#/$defs/commitSha" },
        "candidateTree": { "$ref": "#/$defs/commitSha" },
        "decision": { "enum": ["reproduced", "rejected"] },
        "freshnessExpiresAt": {
          "oneOf": [{ "$ref": "#/$defs/rfc3339Timestamp" }, { "type": "null" }]
        },
        "execution": { "$ref": "#/$defs/bundleExecution" },
        "intakeExecutions": {
          "type": "array",
          "maxItems": 20,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/bundleExecution" }
        },
        "kind": { "const": "bundle-review" },
        "reviewedAt": { "$ref": "#/$defs/rfc3339Timestamp" },
        "subjectChecksum": { "$ref": "#/$defs/sha256Integrity" },
        "workPackage": { "$ref": "#/$defs/workPackage" }
      }
    },
    "commitSha": {
      "type": "string",
      "pattern": "^[a-f0-9]{40}(?![\\s\\S])",
      "maxLength": 40
    },
    "bundleExecution": {
      "type": "object",
      "additionalProperties": false,
      "required": ["attempt", "id", "runner"],
      "properties": {
        "attempt": { "type": "integer", "minimum": 1, "maximum": 1000 },
        "id": { "$ref": "#/$defs/stableId" },
        "runner": {
          "type": "string",
          "minLength": 1,
          "maxLength": 200,
          "pattern": "^[^\\u0000-\\u001F\\u007F]+(?![\\s\\S])"
        }
      }
    },
    "gateSubject": {
      "type": "object",
      "additionalProperties": false,
      "required": ["candidateCommit", "decidedAt", "decision", "gate", "kind", "subjectChecksum"],
      "properties": {
        "candidateCommit": { "$ref": "#/$defs/commitSha" },
        "decidedAt": { "$ref": "#/$defs/rfc3339Timestamp" },
        "decision": { "enum": ["pass", "fail"] },
        "gate": { "enum": ["A", "B"] },
        "kind": { "const": "gate-review" },
        "subjectChecksum": { "$ref": "#/$defs/sha256Integrity" }
      }
    },
    "externalSubject": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "bundleId",
        "candidateCommit",
        "candidateTree",
        "decision",
        "execution",
        "externalCommit",
        "externalRepository",
        "externalSubjectId",
        "externalTree",
        "freshnessExpiresAt",
        "kind",
        "reviewedAt",
        "runStartedAt",
        "subjectChecksum",
        "workPackage",
        "workflowDigest",
        "workflowRunAttempt",
        "workflowRunId"
      ],
      "properties": {
        "bundleId": { "$ref": "#/$defs/bundleId" },
        "candidateCommit": { "$ref": "#/$defs/commitSha" },
        "candidateTree": { "$ref": "#/$defs/commitSha" },
        "decision": { "const": "accepted" },
        "execution": { "$ref": "#/$defs/runExecution" },
        "externalCommit": { "$ref": "#/$defs/commitSha" },
        "externalRepository": {
          "type": "string",
          "pattern": "^https://github\\.com/[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+(?![\\s\\S])",
          "maxLength": 240
        },
        "externalSubjectId": { "$ref": "#/$defs/stableId" },
        "externalTree": { "$ref": "#/$defs/commitSha" },
        "freshnessExpiresAt": { "$ref": "#/$defs/rfc3339Timestamp" },
        "kind": { "const": "external-review" },
        "reviewedAt": { "$ref": "#/$defs/rfc3339Timestamp" },
        "runStartedAt": { "$ref": "#/$defs/rfc3339Timestamp" },
        "subjectChecksum": { "$ref": "#/$defs/sha256Integrity" },
        "workPackage": { "$ref": "#/$defs/workPackage" },
        "workflowDigest": { "$ref": "#/$defs/sha256Integrity" },
        "workflowRunAttempt": { "type": "integer", "minimum": 1, "maximum": 1000 },
        "workflowRunId": { "type": "integer", "minimum": 1 }
      }
    },
    "identity": {
      "type": "string",
      "pattern": "^github/[1-9][0-9]{0,19}/[A-Za-z0-9](?:[A-Za-z0-9-]{0,37}[A-Za-z0-9])?(?![\\s\\S])",
      "maxLength": 80
    },
    "manualSubject": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "bundleId",
        "candidateCommit",
        "candidateTree",
        "criterionId",
        "decision",
        "evidenceClass",
        "execution",
        "kind",
        "laneId",
        "procedureChecksum",
        "procedureId",
        "runStartedAt",
        "subjectChecksum",
        "workPackage"
      ],
      "properties": {
        "bundleId": { "$ref": "#/$defs/bundleId" },
        "candidateCommit": { "$ref": "#/$defs/commitSha" },
        "candidateTree": { "$ref": "#/$defs/commitSha" },
        "criterionId": { "$ref": "#/$defs/stableId" },
        "decision": { "enum": ["accepted", "rejected"] },
        "evidenceClass": {
          "enum": ["accessibility", "manual-decision"]
        },
        "execution": { "$ref": "#/$defs/runExecution" },
        "kind": { "const": "manual-review" },
        "laneId": { "$ref": "#/$defs/stableId" },
        "procedureChecksum": { "$ref": "#/$defs/sha256Integrity" },
        "procedureId": { "$ref": "#/$defs/stableId" },
        "runStartedAt": { "$ref": "#/$defs/rfc3339Timestamp" },
        "subjectChecksum": { "$ref": "#/$defs/sha256Integrity" },
        "workPackage": { "$ref": "#/$defs/workPackage" }
      }
    },
    "repositoryRelativePath": {
      "type": "string",
      "pattern": "^[A-Za-z0-9@][A-Za-z0-9._@-]*(?:/[A-Za-z0-9@][A-Za-z0-9._@-]*)*(?![\\s\\S])",
      "maxLength": 240
    },
    "runExecution": {
      "type": "object",
      "additionalProperties": false,
      "required": ["attempt", "id", "runId", "runner"],
      "properties": {
        "attempt": { "type": "integer", "minimum": 1, "maximum": 1000 },
        "id": { "$ref": "#/$defs/stableId" },
        "runId": { "$ref": "#/$defs/stableId" },
        "runner": {
          "type": "string",
          "minLength": 1,
          "maxLength": 200,
          "pattern": "^[^\\u0000-\\u001F\\u007F]+(?![\\s\\S])"
        }
      }
    },
    "reviewer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["identity", "independent", "roles"],
      "properties": {
        "identity": { "$ref": "#/$defs/identity" },
        "independent": { "type": "boolean" },
        "roles": {
          "type": "array",
          "minItems": 1,
          "maxItems": 5,
          "uniqueItems": true,
          "items": {
            "enum": ["general", "accessibility", "compatibility", "data-integrity", "security"]
          }
        }
      }
    },
    "rfc3339Timestamp": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(?:\\.[0-9]{1,9})?(?:Z|[+-][0-9]{2}:[0-9]{2})(?![\\s\\S])",
      "maxLength": 40
    },
    "sha256Integrity": {
      "type": "string",
      "pattern": "^sha256-[A-Za-z0-9+/]{42}[AEIMQUYcgkosw048]=(?![\\s\\S])",
      "maxLength": 60
    },
    "stableId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]*(?![\\s\\S])",
      "not": { "enum": ["__proto__", "prototype", "constructor"] },
      "maxLength": 240
    },
    "workPackage": {
      "type": "string",
      "pattern": "^(?:M[1-6]-[0-9]{2}|ST-(?:[0-9]|1[01]))$(?![\\s\\S])",
      "maxLength": 5
    }
  }
}
