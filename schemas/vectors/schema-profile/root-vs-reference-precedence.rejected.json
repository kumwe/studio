{
  "contractVersion": "0.1-draft",
  "kind": "schema-profile-vector",
  "id": "schema-profile/root-vs-reference-precedence",
  "description": "A root closure failure precedes an unresolved reference under a later property member.",
  "profile": "studio.profile/schema-property",
  "schema": {
    "additionalProperties": true,
    "properties": {
      "x": { "$ref": "#/$defs/missing" }
    },
    "type": "object"
  },
  "expect": {
    "outcome": "rejected",
    "code": "invalid-root",
    "schemaPath": "/additionalProperties"
  }
}
