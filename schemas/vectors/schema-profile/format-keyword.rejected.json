{
  "contractVersion": "0.1-draft",
  "kind": "schema-profile-vector",
  "id": "schema-profile/format-keyword",
  "description": "The alpha property profile has no format registry, so format cannot become an implementation-dependent assertion.",
  "profile": "studio.profile/schema-property",
  "schema": {
    "type": "object",
    "additionalProperties": false,
    "properties": { "email": { "type": "string", "format": "email" } }
  },
  "expect": {
    "outcome": "rejected",
    "code": "unsupported-keyword",
    "schemaPath": "/properties/email/format"
  }
}
