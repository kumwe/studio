{
  "contractVersion": "0.1-draft",
  "kind": "schema-profile-vector",
  "id": "schema-profile/diagnostic-precedence",
  "description": "Unordered schema maps use ascending UTF-16 code-unit order for the first admission diagnostic.",
  "profile": "studio.profile/schema-property",
  "schema": {
    "additionalProperties": false,
    "properties": {
      "z": {
        "type": "string",
        "format": "email"
      },
      "a": {
        "type": "string",
        "format": "email"
      }
    },
    "type": "object"
  },
  "expect": {
    "outcome": "rejected",
    "code": "unsupported-keyword",
    "schemaPath": "/properties/a/format"
  }
}
